{
  "name": "milliseconds",
  "displayName": "Milliseconds",
  "description": "Insert Milliseconds",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.54.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:insert-time.insertCurrentTime"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "insert-time.insertCurrentTime",
        "title": "Insertar Millisegundos"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.54.0",
    "@types/mocha": "^10.0.8",
    "@types/node": "20.x",
    "eslint": "^9.11.1",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1"
  },
  "repository": {
    "type": "git",
    "url": "https: //github.com/axeleszu/milliseconds.git"
  }
}